export enum AuthError {
    USER_ALREADY_REGISTERED = 'User already registered',
    EMAIL_ALREADY_REGISTERED = 'Email already registered',
    CANNOT_REGISTER = 'Cannot register user',
    INVALID_CREDENTIALS = 'INVALID_CREDENTIALS'
}  